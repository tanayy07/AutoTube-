# Telegram Bot Configuration (Optional - only if using Telegram bot)
TELEGRAM_BOT_TOKEN=your_bot_token_here
WEBHOOK_URL=https://your-domain.com/api/telegram

# Database Configuration (Optional - only if using database features)
DATABASE_URL=file:./youtube-downloader.db

# Download Configuration
# Maximum file size in MB (default: 50MB for Telegram's limit)
MAX_FILE_SIZE_MB=50

# Temporary directory for downloads (optional, defaults to system temp)
TEMP_DIR=/tmp/ytdl

# FFmpeg Configuration (REQUIRED for video processing)
# Windows example: C:\Users\YourName\AppData\Local\Microsoft\WinGet\Packages\Gyan.FFmpeg_Microsoft.Winget.Source_8wekyb3d8bbwe\ffmpeg-8.0-full_build\bin
# Mac/Linux example: /usr/local/bin/ffmpeg
FFMPEG_PATH=path_to_ffmpeg_here

# IMPORTANT: For high-quality downloads (720p+), you need:
# 1. Create a cookies.txt file in the root directory
# 2. Get cookies using "Get cookies.txt LOCALLY" browser extension
# 3. The cookies.txt file is gitignored and should never be committed

# Application Configuration
NODE_ENV=development
PORT=3000

# Optional Features
ENABLE_ANALYTICS=false
LOG_LEVEL=info
